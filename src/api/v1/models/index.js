// ----------Imports----------
const UserModel = require("./User");
const UserTokenModel = require("./UserToken");

// ----------Exports----------
module.exports = {
  UserModel,
  UserTokenModel,
};
